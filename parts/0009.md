# Draw Detection
Chess positions can be drawn by the fifty move rule or by threefold repetition. We add `Position::is_draw()` to check both, using the halfmove clock for the fifty move rule and the history of Zobrist hashes to detect a position repeated three times.
